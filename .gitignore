# Secrets — NEVER commit
.env
*.env
!.env.example

# lost-finder hunt output — may hold COPIES OF RECOVERED FILES & WALLET SECRETS. Never commit.
found/
agents/*/found/
**/found/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
*.egg-info/
.pytest_cache/

# uv
uv.lock

# OS
Thumbs.db
.DS_Store

# Generated image scratch / logs / usage ledger
agents/*/output/
agents/*/.cache/
agents/*/.usage/
*.log

# LLM Wiki raw assets that are large (keep markdown, ignore heavy binaries if desired)
# (commented out by default — uncomment if raw/ gets heavy)
# llm-wiki/raw/assets/
